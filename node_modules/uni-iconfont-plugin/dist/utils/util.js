"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hyphen2Hump = exports.generateCase = void 0;
var tslib_1 = require("tslib");
var replace_1 = require("./replace");
var ATTRIBUTE_FILL_MAP = ['path'];
var generateCase = function (data, config) {
    var e_1, _a;
    var template = "<svg viewBox='".concat(data.$.viewBox, "' xmlns='http://www.w3.org/2000/svg' width='${svgSize}' height='${svgSize}'>");
    var _loop_1 = function (domName) {
        if (domName === '$') {
            return "continue";
        }
        var counter = {
            colorIndex: 0,
        };
        if (data[domName].$) {
            template += "<".concat(domName).concat(addAttribute(domName, data[domName], counter, config), " />");
        }
        else if (Array.isArray(data[domName])) {
            data[domName].forEach(function (sub) {
                template += "<".concat(domName).concat(addAttribute(domName, sub, counter, config), " />");
            });
        }
    };
    try {
        for (var _b = tslib_1.__values(Object.keys(data)), _c = _b.next(); !_c.done; _c = _b.next()) {
            var domName = _c.value;
            _loop_1(domName);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    template += "</svg>";
    return template.replace(/<|>/g, function (matched) { return encodeURIComponent(matched); });
};
exports.generateCase = generateCase;
var addAttribute = function (domName, sub, counter, config) {
    var e_2, _a;
    var template = '';
    if (sub && sub.$) {
        if (ATTRIBUTE_FILL_MAP.includes(domName)) {
            // Set default color same as in iconfont.cn
            // And create placeholder to inject color by user's behavior
            sub.$.fill = sub.$.fill || '#333333';
        }
        try {
            for (var _b = tslib_1.__values(Object.keys(sub.$)), _c = _b.next(); !_c.done; _c = _b.next()) {
                var attributeName = _c.value;
                if (attributeName === 'fill') {
                    var color = void 0;
                    var keyword = void 0;
                    if (config === null || config === void 0 ? void 0 : config.hexToRgb) {
                        color = (0, replace_1.replaceHexToRgb)(sub.$[attributeName]);
                        keyword = 'colors';
                    }
                    else {
                        keyword = 'color';
                        color = sub.$[attributeName];
                    }
                    template += " ".concat(attributeName, "='${(isStr ? ").concat(keyword, " : ").concat(keyword, "[").concat(counter.colorIndex, "]) || '").concat(color, "'}'");
                    counter.colorIndex += 1;
                }
                else {
                    template += " ".concat(attributeName, "='").concat(sub.$[attributeName], "'");
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_2) throw e_2.error; }
        }
    }
    return template;
};
var hyphen2Hump = function (str) {
    return str.replace(/-(\w)/g, function (_match, letter) { return letter.toUpperCase(); });
};
exports.hyphen2Hump = hyphen2Hump;
