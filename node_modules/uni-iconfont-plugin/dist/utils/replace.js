"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.replaceHexToRgb = void 0;
var replaceHexToRgb = function (hex) {
    var rgb = [];
    //去除前缀 # 号
    hex = hex.substr(1);
    if (hex.length === 3) {
        // 处理 '#abc' 成 '#aabbcc'
        hex = hex.replace(/(.)/g, '$1$1');
    }
    hex.replace(/../g, function (color) {
        // 按16进制将字符串转换为数字
        rgb.push(parseInt(color, 0x10));
        return color;
    });
    return 'rgb(' + rgb.join(',') + ')';
};
exports.replaceHexToRgb = replaceHexToRgb;
